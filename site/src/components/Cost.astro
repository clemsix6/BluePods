---
const consequences = [
  {
    title: 'No horizontal scaling',
    body: 'Total capacity is bounded by what one node can ingest and validate, not by the sum of all nodes. Adding validators spreads storage and execution around, but it does not raise the ceiling.',
  },
  {
    title: 'It halts rather than fork',
    body: 'Under a network partition the system is CP. It stops instead of risking a rollback, so a global partition can take the cloud down until it heals.',
  },
];
---

<section class="band band--tint" id="cost">
  <div class="wrap reveal">
    <div class="split">
      <h2>The part we don't hide.</h2>

      <div class="prose">
        <p>
          Holding both guarantees requires a single global DAG with one total order. Every
          operation, from every app, passes through it and is seen by every validator, even
          though each validator only stores and executes a fraction of the state.
        </p>
        <p>Two things follow, and neither is a footnote.</p>
      </div>
    </div>

    <div class="pair">
      {
        consequences.map((item) => (
          <article>
            <h3>{item.title}</h3>
            <p>{item.body}</p>
          </article>
        ))
      }
    </div>

    <p class="closer">
      The bet that makes this worth taking is bandwidth. Per-node bandwidth keeps climbing,
      and a data availability split that puts only hashes on the consensus path makes the
      same bet easier to win without giving up the total order.
    </p>
  </div>
</section>

<style>
  /* Two consequences, side by side, parted by a rule. No boxes: the band's own
     colour is the only surface this section gets. */
  .pair {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    margin-top: clamp(3rem, 6vw, 5rem);
    padding-top: clamp(1.75rem, 3vw, 2.5rem);
    border-top: 1px solid var(--rule-tint);
  }

  .pair > * {
    padding-right: clamp(1.5rem, 4vw, 3.5rem);
  }

  .pair > * + * {
    padding-right: 0;
    padding-left: clamp(1.5rem, 4vw, 3.5rem);
    border-left: 1px solid var(--rule-tint);
  }

  h3 {
    margin-bottom: 1rem;
  }

  .pair p {
    max-width: 42ch;
    color: var(--ink-tint);
  }

  .closer {
    max-width: 62ch;
    margin-top: clamp(2.5rem, 5vw, 4rem);
    padding-top: 1.5rem;
    border-top: 1px solid var(--rule-tint);
    color: var(--ink-tint);
  }

  @media (max-width: 720px) {
    .pair {
      grid-template-columns: minmax(0, 1fr);
    }

    .pair > * {
      padding-right: 0;
    }

    .pair > * + * {
      margin-top: 2.5rem;
      padding-top: 2.5rem;
      padding-left: 0;
      border-top: 1px solid var(--rule-tint);
      border-left: 0;
    }
  }
</style>
